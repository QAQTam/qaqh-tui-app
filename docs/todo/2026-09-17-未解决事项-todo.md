# TUI 未解决事项台账（单一台账）（2026-09-17）

## 0. 元信息

| 项 | 值 |
|---|---|
| 台账日期 | 2026-09-17 |
| 基准代码 | TUI `809e317`（main，工作树干净）；后端 `qaqh-backend` main；winui `9c6e588`（2026-09-13） |
| 远端 | `https://cnb.cool/QAQ-Harness/qaqh-tui-app`（CNB；issue + PR 即本台账的执行面） |
| 门禁 | `cargo test --all-targets`；`cargo clippy --all-targets -- -D warnings`；本仓 `cargo fmt --all --check`。CNB `main.push` 跑 Linux CI，`pull_request` 跑 NPC 自动审查（见 `.cnb.yml`） |
| 状态图例 | `OPEN` 待修 ｜ `DOING` 已开工作树/分支 ｜ `BLOCKED` 等外部条件 ｜ `DECIDE` 待决策 ｜ `DONE` 已闭环 ｜ `INVALID` 已核实不成立 |

## 0b. 这份文档为什么存在

本项目此前有**三本互不引用的账**，导致「还有哪些 bug 没解决」无法一眼回答：

1. `docs/buglist/2026-09-15-TUI已知缺陷跟踪-buglist.md`——T-xx / D-x / B1-B2 / C1-C3 / D1-D4 / E1-E3。
2. `docs/report/2026-09-14-TUI连接生命周期401卡死-report.md`——D-1~D-7 / O-1~O-6。
3. `BUGLIST.md`（仓库根）——CodeGraph 静态排查，编号 BUG-001~010，自述「**未读取 docs**」，与前两份**零交集**。

**自本台账起，未解决事项只在这里登记**：上述三份保留为历史与证据来源（已闭环的明细交给 git 历史），新条目一律进本文件。跨仓条目以**后端登记册为准**，本文件只做指向与 TUI 侧影响面说明。

## 1. 在修（已开工作树 / 分支）

| ID | 级别 | 事项 | issue | 分支 / 工作树 | 状态 |
|---|---|---|---|---|---|
| **U-01** | P1 | 子代理生命周期三处缺陷：`TimelineLost` 失败误标 `Closed`；`derive_terminal` 派生终态不停止跟踪；父 seed 非本地 tab 时不回收 | #2 | `fix/subagent-lifecycle` / `qaqh-tui-app-wt-subagent` | DOING |
| **U-02** | P2 | 状态与弹层可见性：`StreamIssue` 在 `Ready` 相位永不显示；切标签不清 overlay（确认动作会作用到旧 seed）；权限面板可被补投成幽灵 | #3 | `fix/status-and-overlays` / `qaqh-tui-app-wt-ui` | DOING |
| **U-03** | P2 | 会话创建失败无终态反馈（`Rejected` 不清 `pending_creates` → 假 `creating…` 最长 15s）；`progress_truncated` 是死字段 | #4 | `fix/create-feedback` / `qaqh-tui-app-wt-session` | DOING |
| **U-04** | P2 | 依赖清理：`reqwest` / `sha2` / `windows-sys` 已零引用仍在依赖图（`native-tls` 只因本仓声明被拉入）；`serde_json` 保序 feature 相加的行为回归从未做 | #5 | `chore/deps-cleanup` / `qaqh-tui-app-wt-deps` | DOING |
| **U-05** | P2 | 文档清理：三本账合并为本台账、根 `BUGLIST.md` 归档、订正 `report:21` 的 D-5 状态、闭合阶段二验收判据、复测快照数字 | #6 | `docs/unresolved-ledger` / `qaqh-tui-app-wt-docs` ｜ **PR #14** | DOING |

## 2. 待人工验证（需真机或环境，代码侧无法自证）

| ID | 级别 | 事项 | issue | 现状与证据 | 状态 |
|---|---|---|---|---|---|
| **U-06** | P2 | **租约过期自愈**：会话活着时让租约失效，期望自动重新 open，不出现永久 `✗ lost` | #7 | 无测试、无 harness。`scripts/e2e-restart.sh` 只做「杀 daemon → 起新 daemon」与 `Ctrl+R`，**不含**租约失效构造。台账 `:164` 记 ❌ | OPEN |
| **U-07** | P2 | **`Lagged` 终止帧恢复**：压出 SSE 缓冲溢出，期望显示「服务端终止流（lagged），重连重定基」并恢复 | #7 | `rg Lagged scripts/` 零命中。台账 `:165` 记 ❌ | OPEN |
| **U-08** | P2 | **多标签 + 子代理并发**：多 seed timeline 的直接验收（本批唯一新增能力） | #7 | 单测与真机都没跑；对端 `crates/qaqh-client/tests/lease_renegotiation.rs:410` 仍 `#[ignore]`。台账 `:166` 记 ❌ | OPEN |
| **U-09** | P2 | 两个 `#[ignore]` 集成测试未补跑（handoff 明写的待补跑命令从未执行） | #8 | `lease_renegotiation.rs:185` / `:410` 仍 `#[ignore]`；该命令在两仓 docs 再无第二次出现。**注意这两个测试在后端 crate**，台账 `:170` 的「本仓」措辞易误读 | OPEN |

> U-06/U-07 已具备补跑条件（阻塞它们的后端 `BUG-2026-09-15-02` 已由 `674742f` 修掉，harness 也已存在）——它们从「被阻塞」变成了「无人认领」，这正是本台账要暴露的问题。
> U-08 若确实需要真实 LLM 后端，请显式标注为「环境不具备，长期挂起」，不要再以模糊的「未验证」留存。

## 3. 跨仓 / 需后端配合

| ID | 级别 | 事项 | issue | 说明 | 状态 |
|---|---|---|---|---|---|
| **U-10** | P2 | 工具结果消费面契约整份未开工 | #10 | TUI 侧与后端对偶侧均 0 实现。**实测事实**：会话 `5e34cca4` 的 2705 个工具块中 **2363 个（87.4%）的 `summary` 是被截断的 JSON**，1547 条长度恰为 512 → 连 JSON 都不合法。TUI 侧 8 个补偿函数约 268 行 + 6 处 `serde_json::from_str` 全在。后端根因 `qaqh-runtime/src/timeline.rs:118-126` 原封不动 | BLOCKED（等后端类型化） |
| **U-11** | P2 | 后端登记册里的 TUI 侧条目 `BUG-2026-09-17-06` 机制描述与代码不符 | #2 覆盖 | 其所述 `apply_status` 不 untrack **不成立**（`mod.rs:844-856` 确实调 `untrack_subagent`，自 `f85c33d` 就在）；**真实缺口**是 `subagent.rs:381` 的 `derive_terminal` 派生路径，已并入 U-01 | DOING（并入 U-01） |
| **U-12** | P3 | 后端登记册补标「已移交本仓」，消除双份记账 | #9 | 实测 `rg '已移交' qaqh-backend/docs/` **零命中**；三条待标注项里只有 `O-2` 实质被交叉引用 | OPEN |
| **U-13** | P3 | winui 退役 `compute_stall` / `rebuild_client`（handoff 自述「未执行」） | #11 | winui HEAD 停在 `9c6e588`（2026-09-13，早于该 handoff）；两函数仍在 `core_client.rs:585` / `:638` | OPEN |
| **U-14** | P3 | `plan §3.1` 方法缺口：`session.meta` / `plan.read` / `plan.context_stats` / `stats.token_usage` / `git.*` | #13 | 对端封闭枚举仍无这些方法；本仓方法名常量表已随 `d379d90` 整表删除 → 将来要用得从头加 | DECIDE（永久不需要 or 登记为后端 API 缺口） |
| **U-22** | **P1** | **CI 的 `linux-ci` 自 2026-09-15 起恒失败**，`main` 实际没有可用门禁 | #15 | `Cargo.toml` 的 path 依赖（`../qaqh-backend`、`../ratatui`）在 CNB 工作区解析为 `/ratatui/ratatui` 等**不存在的路径** → `cargo` 报 `No such file or directory`。09-14 的 `ba26c8f` 还是 success（当时无 path 依赖），09-15 迁移引入后就没绿过。需决策「流水线内联 checkout / 发布制品 / 烘进镜像」 | OPEN |

## 4. 需决策

| ID | 级别 | 事项 | issue | 待决内容 | 状态 |
|---|---|---|---|---|---|
| **U-15** | P3 | 前端开发标准缺「凭据类错误不得终止生命周期」约束 | #12 | 迁移后该不变式的落点已上移到 `qaqh-client`，TUI 侧 `is_fatal`/`is_credential`/`supervisor_action` 已删 → **不变式无处安放**。需决定由哪份文档承载 | DECIDE |
| **U-16** | P3 | `BUG-004` 潜在隐患：`/` 菜单 `take(6)` 与实际候选数不一致 | — | 当前 slash 命令只有 **3** 个，`take(6)` 永不截断 → **不可达**。但一旦命令超过 6 条，高亮会漂移。属潜在项，是否顺手加个断言由 U-02 决定 | DECIDE（低优先） |
| **U-17** | P3 | `BUG-009` 设置页 Draft 与 `ConfigChanged` 并发覆盖 | — | 是否真的会覆盖并发修改，取决于后端 `ConfigSave{fields}` 是否为 merge patch → **需后端语义确认**，前端无法单方面判定 | DECIDE（等后端） |

## 5. 本轮已闭环

| ID | 事项 | 闭环方式 |
|---|---|---|
| **U-18** | 根 `BUGLIST.md` BUG-001（渲染缓存冻结）+ BUG-010（`show_reasoning` 未进缓存键） | `77dd2fd`：transcript 分段渲染缓存与虚拟化；顺带修掉缓存键宽度错位（`main.rs` 传终端全宽 vs `ui::draw` 用 chat 区宽）与压缩动画冻结（`CompactStarted/Progress/Finished` 不清缓存）。含回归锁 `ui::tests::cache_key_width_matches_transcript_draw_width` |
| **U-19** | `report:21` 的 D-5 状态失真 | 本批订正（见 `docs/report/2026-09-14-TUI连接生命周期401卡死-report.md`） |
| **U-20** | 阶段二验收判据 `rg "crate::protocol"` 零命中（字面永不成立） | 本批改写为现实判据（见 `docs/plan/2026-09-14-TUI传输层迁移qaqh-client-plan.md`） |
| **U-21** | 本仓 `cargo fmt --all --check` 不干净（`src/app/markdown.rs` 3 处） | `809e317` 前一批定向 rustfmt 修掉 |

## 6. 已核实**不成立**（留档，避免重复上报）

| 项 | 原述 | 核实结论 |
|---|---|---|
| `BUGLIST.md` BUG-001 第 3 点 | 「大量代码块/工具块被整块挤掉」 | **不成立**。`MD_BLOCK_LINE_CAP` 是**单块内**截断且带「（内容省略 N 行）」可见标注，全函数无整行级 `truncate` 或全局上限 |
| `BUGLIST.md` BUG-002 主述 | 「renderer 与 App 层对 top overlay 判断不一致」 | **不成立**。两处都取 `overlays.last()`（`ui/overlays.rs:14`、`overlay_ops.rs:127`）。真实残留是切标签不清 overlay（并入 U-02） |
| `BUGLIST.md` BUG-003 第 1 点 | 「窗口期内用户没有任何反馈」 | **不成立**。`ui/status_bar.rs:40` 在 `pending_creates` 非空时显示 `· creating…`。真实问题是失败后不清 map（并入 U-03） |
| `BUGLIST.md` BUG-003 第 2 点 | 「command_id 复用不会去重」 | **不成立**。`session_ops.rs:73` 每次 `Uuid::new_v4()`，全仓唯一 insert 点 |
| `BUGLIST.md` BUG-004 | 「`/` 菜单高亮漂移」（标 `confirmed`） | **当前不可达**。slash 命令仅 3 个，`take(6)` 永不截断（降级为潜在项 U-16） |
| `report` O-4 | 「daemon 日志 0 字节」 | **本机不成立**。日志 1.7 MB 且在持续写入；属环境问题，非代码缺陷 |
| `report` D-7 / O-3 | 「后端 HEAD 编译失败 11 error」 | **已不成立**。`cargo check -p qaqh-runtime --lib` exit 0 |
| `handoff §2.3` | 「`reqwest` 已随 `http.rs` 一并删除」 | **与代码相反**。`Cargo.toml:33` 仍在，`src/` 零引用（并入 U-04） |

---

## 7. Checklist

> 勾选即代表「有可复现证据」：代码项=门禁全绿 + 回归锁；真机项=脚本跑通并回填结论；文档项=结论已落到对应文件。

### 7.1 在修（本轮工作树）

- [ ] **U-01** 子代理生命周期三处缺陷 → issue #2 / `fix/subagent-lifecycle`
  - [ ] `TimelineLost` 仅在 404 时映射 `Closed`，其他错误保留状态并提示
  - [ ] 所有终态路径（权威事件 / 派生兜底 / 404）都停止 timeline 跟踪
  - [ ] 父 seed 非本地 tab 时也能回收子代理
  - [ ] 三条各配回归测试；门禁全绿
- [ ] **U-02** 状态与弹层可见性 → issue #3 / `fix/status-and-overlays`
  - [ ] `Ready` 相位下的 `StreamIssue` 可见
  - [ ] 切标签清理不属于该 seed 的 overlay；确认类 overlay 不跨标签生效
  - [ ] 同一 `tool_call_id` 已响应后不再入队
- [ ] **U-03** 创建失败反馈 + 死字段 → issue #4 / `fix/create-feedback`
  - [ ] `Rejected` 立即清 `pending_creates` 并提示失败
  - [ ] `progress_truncated` 接上消费面或连同 wire 字段删除（二选一，不留死字段）
- [ ] **U-04** 依赖清理 + 保序回归 → issue #5 / `chore/deps-cleanup`
  - [ ] 移除 `reqwest` / `sha2` / `windows-sys`（**必须实测**删除后编译与门禁）
  - [ ] `cargo tree` 不再出现本仓拉入的 `native-tls` 与 `sha2 0.10`
  - [ ] 保序行为补回归锁，或记录「无需保序」的结论
- [ ] **U-05** 文档清理 → issue #6 / `docs/unresolved-ledger` ｜ PR #14
  - [x] 新增本台账（含命名模板文件）
  - [x] 根 `BUGLIST.md` 归档进 `docs/buglist/` 并标注不成立条目
  - [x] `docs/buglist/` 去掉已闭环明细，压缩为闭环索引
  - [x] 订正 `report:21` 的 D-5 状态（D-6/D-7 一并对齐）
  - [x] 阶段二验收判据改写为现实可执行
  - [x] 台账快照数字按当前 HEAD 复测更新

### 7.2 待人工验证（需环境）

- [ ] **U-06** 租约过期自愈：构造租约失效并跑通
- [ ] **U-07** `Lagged` 终止帧恢复：压出缓冲溢出并跑通
- [ ] **U-08** 多标签 + 子代理并发：跑通，或显式标注「环境不具备，长期挂起」
- [ ] **U-09** 补跑 `cargo test -p qaqh-client --test lease_renegotiation -- --ignored` 并回填结果

### 7.3 跨仓 / 需决策

- [ ] **U-10** 工具结果消费面契约：后端落地类型化投影 → TUI 删 268 行补偿代码（`serde_json::from_str` 归 0）
- [ ] **U-11** 纠正后端 `BUG-2026-09-17-06` 的机制描述（并入 U-01 后回填）
- [ ] **U-12** 后端登记册补标「已移交本仓」
- [ ] **U-13** winui 评估 `compute_stall` / `rebuild_client` 是否退役
- [ ] **U-14** 决定 `session.meta` / `plan.*` / `stats.*` / `git.*` 是永久不需要还是登记为后端缺口
- [ ] **U-15** 决定「凭据类错误不得终止生命周期」由哪份文档承载
- [ ] **U-16** 决定是否为 slash `take(6)` 加断言（潜在项）
- [ ] **U-17** 确认后端 `ConfigSave{fields}` 是否 merge patch（决定 `BUG-009` 是否成立）
- [ ] **U-22** 修 CI：让 `linux-ci` 能拿到 `qaqh-backend` 与 `ratatui`（版本需显式固定），恢复 `main` 的 test/clippy 门禁
